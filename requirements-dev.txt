# Dev / test tooling (installed on top of requirements.txt)
pytest>=8.2
pytest-asyncio>=0.23
ruff>=0.5          # lint
mypy>=1.10         # type-check the legitimacy core hardest
httpx>=0.27        # TestClient for FastAPI
