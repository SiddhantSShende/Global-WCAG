# ─────────────────────────────────────────────────────────────
# Accessibility Compliance Audit Platform — Python backend deps
# Target runtime: Python 3.11+ inside WSL2 Ubuntu / Docker.
# (Pins are compatible-release ranges; a lockfile is produced in CI.)
# ─────────────────────────────────────────────────────────────

# ── Web API ──
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9          # file uploads (APK/AAB/.app)
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Job queue / async ──
celery[redis]>=5.4,<6.0
redis>=5.0,<6.0                  # client works against Redis or Valkey

# ── Persistence ──
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
alembic>=1.13,<2.0

# ── Object storage (S3 / MinIO / Garage — all S3-compatible) ──
boto3>=1.34,<2.0

# ── Browser automation + evidence ──
playwright>=1.45                 # `playwright install --with-deps chromium`
Pillow>=10.3                     # screenshot crop / highlight / downscale

# ── Recon / discovery helpers (Go tools are vendored separately) ──
httpx>=0.27,<1.0                 # Python HTTP client for CT-log APIs
tldextract>=5.1                  # robust domain/subdomain parsing
beautifulsoup4>=4.12             # DOM parsing for template clustering

# ── Report generation ──
python-docx>=1.1
docxtpl>=0.18                    # Word-template driven docx (TOC, styled tables)
openpyxl>=3.1

# ── Secrets / config ──
hvac>=2.1                        # HashiCorp Vault client (Phase 6)

# ── Observability ──
structlog>=24.1

# ── Dev / test (also see requirements-dev.txt) ──
pytest>=8.2
pytest-asyncio>=0.23
